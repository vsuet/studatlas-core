query nsmaTesting {
  academy(academyId: "nsma") {
    id
    groups {
      id
    }
  }
}

query xxx {
  division(academyId: "nsma", divisionId: 1) {
    id
    name
    statistics {
      all
      noCheckpoints
      noRatings
      notClosed
      blank
    }
  }
}
